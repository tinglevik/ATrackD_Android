<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_item_root"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:clickable="true"
    android:focusable="true"
    tools:layout_width="wrap_content">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <!-- TextView для Emoji-иконок (гарантирует видимость в шторке и на локскрине) -->
        <TextView
            android:id="@+id/activity_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="22sp"
            android:gravity="center"
            android:includeFontPadding="false"
            tools:text="🍎" />

        <ImageView
            android:id="@+id/activity_active_indicator"
            android:layout_width="4dp"
            android:layout_height="4dp"
            android:layout_marginTop="2dp"
            android:src="@drawable/dot_indicator"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/activity_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Activity" />
    </LinearLayout>

    <ImageView
        android:id="@+id/activity_divider"
        android:layout_width="1dp"
        android:layout_height="32dp"
        android:layout_gravity="center_vertical"
        android:src="@drawable/divider_color"
        android:contentDescription="@null" />

</LinearLayout>
